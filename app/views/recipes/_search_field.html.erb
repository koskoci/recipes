<%= form_with url: "/search", method: :get do |form| %>
  <%= form.label :query, "What's in your fridge?" %>
  <%= form.text_field :query, size: 50, placeholder: "Please separate ingredients with commas", value: params[:query] %>
  <%= form.submit "Search" %>
<% end %>